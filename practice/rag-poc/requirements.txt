# RAG PoC 의존성
# 버전은 여유 범위(>=)로 지정 — 운영 시 lock 파일로 고정 권장.

numpy>=1.24            # 인메모리 코사인 유사도(lite 모드 dense 인덱스)
rank-bm25>=0.2.2       # Sparse(BM25) 검색 — 키워드 축
sentence-transformers>=2.2  # 임베딩 + (선택) CrossEncoder 리랭커
                            # 미설치/네트워크 불가 시 해시 더미 임베딩으로 폴백(코드 안전장치)
ragas>=0.1             # 평가 하니스(RAGAS 4지표) — eval/evaluate.py
datasets>=2.14         # RAGAS 입력용 Dataset 래퍼

# --- 선택(pgvector 운영 경로, MODE="pgvector") ---
# psycopg2-binary>=2.9  # PostgreSQL 드라이버
# pgvector>=0.2         # pgvector 파이썬 바인딩
